"use client";

import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { TrendPoint } from "@/lib/domain";

export function ActivityChart({ data }: { data: TrendPoint[] }) { return <div><div role="img" aria-label={`Activity trend: ${data.map((point) => `${point.date}, ${point.activity} activities`).join("; ")}`} className="h-[260px] w-full"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{ top: 15, right: 10, left: -20, bottom: 0 }}><defs><linearGradient id="activity-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#b5955d" stopOpacity={0.42} /><stop offset="100%" stopColor="#b5955d" stopOpacity={0.03} /></linearGradient></defs><CartesianGrid stroke="#d8d2c4" strokeDasharray="3 4" vertical={false} /><XAxis dataKey="date" tick={{ fontSize: 11, fill: "#686962" }} axisLine={false} tickLine={false} /><YAxis allowDecimals={false} tick={{ fontSize: 11, fill: "#686962" }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ background: "#fffdf8", border: "1px solid #d8d2c4", borderRadius: 10, fontSize: 12 }} /><Area type="monotone" dataKey="activity" stroke="#745725" strokeWidth={2} fill="url(#activity-fill)" /></AreaChart></ResponsiveContainer></div><details className="mt-3 text-xs text-muted"><summary className="cursor-pointer font-semibold">View chart data as a table</summary><table className="mt-3 w-full text-left"><thead><tr><th className="py-1">Date</th><th>Activities</th><th>Approvals</th></tr></thead><tbody>{data.map((point) => <tr key={point.date}><td className="py-1">{point.date}</td><td>{point.activity}</td><td>{point.approvals}</td></tr>)}</tbody></table></details></div>; }
